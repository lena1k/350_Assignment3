#include <iostream>

using namespace std;

class Quiz1
{
public:
    Quiz1(); //constructor
    ~Quiz1();// destructor
    int countVowels(string input);
    int *myArray; // use * when you don't know the size of your array
};
